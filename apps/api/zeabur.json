{
  "$schema": "https://schema.zeabur.app/zeabur.json",
  "build": {
    "type": "nodejs",
    "build_command": "cd ../.. && pnpm install --frozen-lockfile && cd apps/api && npx prisma generate && npx nest build",
    "output_dir": "dist"
  },
  "start": {
    "command": "npx prisma migrate deploy && node dist/main.js"
  }
}
